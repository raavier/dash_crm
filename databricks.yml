# This is a Databricks asset bundle definition for dash_crm.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: dash_crm

# Define the app configuration
resources:
  apps:
    dash_crm:
      name: dash_crm
      description: "Dashboard CRM com Chatbot integrado - Vale"

      # Resources já configurados na UI
      resources:
        - name: sql-warehouse
          permission: CAN_USE

        - name: serving-endpoint
          permission: CAN_QUERY

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-116288240407984.4.azuredatabricks.net

  ## Optionally, there could be 'staging' or 'prod' targets here.
  # prod:
  #   mode: production
  #   workspace:
  #     host: https://adb-116288240407984.4.azuredatabricks.net
